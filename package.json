{
  "name": "skills-repo-tooling",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "lint:commits": "commitlint --from=HEAD~1 --to=HEAD --verbose"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.5.0",
    "@commitlint/config-conventional": "^20.5.0",
    "husky": "^9.1.7"
  }
}
